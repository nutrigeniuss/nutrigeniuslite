/** Locale fijo PE para Lite Calc (sin perfiles multi-país). */
export const getCurrentLocale = (): string => 'es-PE';

export const setFormatCountry = (_code?: string | null): void => {
  /* no-op en Lite Calc */
};
